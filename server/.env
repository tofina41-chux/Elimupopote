# Local Supabase Postgres connection string (from `supabase start` output,
# or your hosted Supabase project's connection string).
DATABASE_URL="postgresql://postgres:awpEHfek4dq2Z)X@db.kifhgoiwnlbazudixkee.supabase.co:5432/postgres"

# Used to sign/verify mock auth JWTs in this MVP.
# Swap for real Supabase JWT verification (JWKS) in production.
JWT_SECRET="dev-only-secret-change-me"

# Optional — if unset, AI course generation falls back to a realistic mock
# generator (see server/src/services/openai.service.ts) so the MVP runs with
# zero external dependencies.
OPENAI_API_KEY=""

PORT=4000
